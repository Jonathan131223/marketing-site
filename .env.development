# App URL for Sign up / Login and all product CTAs (marketing only reads this).
# Dev: point at the local digi-app Vite server (npm run dev @ :8080).
# Override in .env.local to test against the deployed app instead.
# PUBLIC_ prefix so Astro inlines it into both server AND client bundles.
PUBLIC_APP_BASE_URL=http://localhost:8080
